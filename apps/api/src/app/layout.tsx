export const metadata = {
  title: "Pandi CRM API",
  description: "Service endpoints for tasks, AI notes, and rituals.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
